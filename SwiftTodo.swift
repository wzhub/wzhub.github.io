struct todoListItem {
}
// the struct is used to add tags
// but is there a way to filter all the items with specific tag?

let swiftStudy = [
    "create struct for Bear social App",
    "Finish reading AP CS Swift book",
    "reopen GitHub personal page, set a progress bar for 6-weeks project, then"
]
